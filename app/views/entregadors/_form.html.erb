<%= form_with(model: entregador) do |form| %>
  <% if entregador.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(entregador.errors.count, "error") %> prohibited this entregador from being saved:</h2>

      <ul>
        <% entregador.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= form.text_field :nome, class: "form-control", placeholder: "Nome" %>
  </div>

  <div class="field form-group">
    <%= form.text_field :telefone, class: "form-control", placeholder: "Telefone" %>
  </div>

  <div class="field form-group">
    <%= form.text_field :cpf, class: "form-control", placeholder: "CPF" %>
  </div>

  <div class="text-center">
    <%= form.submit class: "btn btn-outline-dark" %>
  </div>
<% end %>
